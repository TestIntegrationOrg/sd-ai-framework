# SD-AI 0.17 Architecture Drift release evidence

This document is the release-readiness evidence map for deterministic Architecture Drift. The authoritative executable gate is the repository's full CI workflow: every pull request and `main` push runs the unfiltered `pytest -q` suite on Ubuntu and Windows with Python 3.11 and 3.12.

## Integrated 0.17 journey

`tests/test_v017_architecture_drift_release_gate.py::test_v017_integrated_architecture_drift_release_journey` builds a real local Git-backed SD-AI project and proves one end-to-end architecture-governance journey without network/provider authority:

1. initialize a feature workspace and project policy;
2. declare approved `api` and `data` components with stable repository ownership;
3. bind an OpenAPI source/operation through the existing 0.16 canonical contract source/symbol hashes;
4. declare a Compose deployment source without invoking Docker;
5. approve required dependency, HTTP communication, contract, data ownership/access, and trust-zone/boundary facts;
6. bind independent typed architecture approval evidence to the exact topology file/hash and topology truth hash;
7. observe dependency imports, service communication/contract truth, SQL data ownership/access, Compose workload placement, and derived trust-boundary reality through the production 0.17 observer registry;
8. prove the complete architecture evaluation JSON/hash is deterministic;
9. prove intentional warning-level deployment drift remains visible but non-blocking under the core `error` delivery threshold;
10. exercise public `sdai architecture drift FEATURE --json` and prove exit 0/allowed output;
11. exercise the public architecture-aware trace export and prove topology/component/fact nodes plus typed approval evidence binding are present in the canonical TraceGraph;
12. remove the required internal dependency/HTTP edge while leaving approved topology/evidence unchanged;
13. prove the same production evaluator deterministically reports error-level required-missing drift and policy outcome `blocked`;
14. exercise public `sdai architecture drift` again and prove exit 2;
15. exercise public trace export again and prove current drift-finding nodes are projected; and
16. exercise public `sdai verify` and prove architecture drift appears as deterministic blocking findings in the existing verification model.

The gate covers dependency, communication, contract, data ownership/access, deployment, trust-boundary/security derivation, policy enforcement, approval evidence, trace projection, verification projection, deterministic ordering/hashing, and provider independence in one production journey.

## Fail-closed and upgrade compatibility

`test_v017_upgrade_compatibility_and_fail_closed_inputs` protects both adoption and authority boundaries:

- projects without approved topology/drift policy remain backward compatible;
- the legacy `specs/<FEATURE>` workspace layout still resolves when architecture governance is not required;
- effective policy can explicitly require topology and then block a project that has none;
- unsafe deployment traversal paths fail closed;
- unknown deployment source kinds fail closed rather than triggering runtime/cloud discovery;
- malformed/unsupported topology input returns the CLI malformed/unsafe exit class rather than “no drift”; and
- stale/mismatched architecture approval remains a delivery blocker and cannot satisfy governance.

Issue-specific 0.17 suites additionally cover ambiguous component ownership, dynamic HTTP/data targets, secret redaction, trust-zone ambiguity, forbidden/gateway/control/sensitive-data crossings, deployment resource ambiguity, policy symlink/path safety, non-weakening policy precedence, and legacy lifecycle routing.

## Historical 0.6–0.16 compatibility

The same full test invocation keeps every historical compatibility/evidence gate enabled. The 0.17 release test explicitly protects the presence of:

- 0.6 release compatibility;
- 0.7 release compatibility;
- 0.8 release compatibility;
- 0.9 release compatibility;
- 0.10 release compatibility;
- 0.11 release evidence;
- 0.12 signed pack lifecycle gate;
- 0.13 integration SDK release gate;
- 0.14 Workflow Engine 2.0 release gate;
- 0.15 authority hardening plus integrated release-readiness gate; and
- 0.16 Contract Engineering integrated release gate.

`test_v017_release_gate_keeps_historical_gates_and_full_matrix_enabled` also verifies `.github/workflows/ci.yml` still declares Ubuntu/Windows × Python 3.11/3.12 and still runs unfiltered `pytest -q`, not a narrowed release subset.

## Public capability and migration reference

`docs/ARCHITECTURE-DRIFT.md` is the 0.17 user/maintainer reference for:

- approved topology schema and stable fact kinds/modes;
- component ownership and observer extension model;
- dependency, communication/contract, data, deployment, and trust-boundary observations;
- secret safety and no-live-probing boundaries;
- typed architecture approval and freshness requirements;
- `sdai architecture drift` CLI exits and JSON/API versions;
- monotonic core -> organization -> repository -> user policy;
- existing TraceGraph and verification integration;
- stable error families;
- intentional limitations; and
- migration guidance, including backward compatibility for projects without approved topology.

## Exact-head evidence protocol

Issue #223 is closed only after its exact pull-request head is green on all four supported CI legs. The exact PR head, workflow run id, and four job conclusions are recorded on issue #223 after that run completes; this avoids mutating the validated head merely to write its own SHA into the repository.

After the PR is squash-merged, the resulting exact merged `main` head must independently complete the same full four-leg CI matrix successfully. The merged-main SHA/run evidence is recorded on parent issue #24 before #24 is closed. This provides a non-circular, auditable release-readiness record for both the reviewed implementation head and the actual released main history.

## Release decision

Issue #223 may close after exact-PR-head validation. Parent #24 may close only after the squash-merged `main` head is green across Ubuntu/Windows × Python 3.11/3.12 and the recorded evidence confirms that the complete 0.17 journey and historical 0.6–0.16 gates ran in that unfiltered suite.
