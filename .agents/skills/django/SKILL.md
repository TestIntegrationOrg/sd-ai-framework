---
name: django
description: Apply Django application engineering practices.
---

# Django

Use the repository's established Django settings, app boundaries, ORM/query patterns, migrations, validation, authentication/authorization, transactions, logging, and test conventions. Preserve compatibility with the detected Django/Python environment. Treat schema and migration changes explicitly and do not upgrade Django or dependencies without approval.
