---
name: typescript-engineering
description: Apply repository-aware TypeScript engineering practices.
---

# TypeScript Engineering

Honor the repository's TypeScript compiler configuration, runtime targets, module format, package manager, type-safety conventions, async/error handling, security controls, and test stack. Avoid weakening types to bypass design problems. Use APIs available to the configured targets and do not upgrade TypeScript or dependencies without approval.
